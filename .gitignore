node_modules/
dist/
.DS_Store
*.local

# ── Backend secrets & generated data (never commit) ──────────────────────────
backend/.env
backend/participants.json
backend/admin.json
backend/uploads/

# ── Frontend env ──────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# docs/ is intentionally tracked — it is the GitHub Pages build output.
# Run: npm run build && git add docs && git commit -m "build" && git push
